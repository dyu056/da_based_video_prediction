# Experiment Report: `da_information_curve_refresh_v1`

## Goal

Rerun the epoch-by-epoch latent ETKF evaluator on the saved L40 host checkpoints so the output includes the DA information metrics across epochs: entropy, relative entropy, and mutual information.

## Date

- Start time: `2026-04-15 18:01:48`

## Status

Running.

## Experiment Type

- Category: epoch-curve metric refresh
- Stage 1 model: `OpenLoopVideoPredictor`
- Stage 2 method: latent `ETKF`
- Dataset: `Moving MNIST`
- Task: first `10` frames to next `10` frames prediction

## Submission Metadata

- Server: `172.16.43.21`
- SSH port: `2222`
- User: `yudanni`
- Partition: `compute`
- GPU request: `1 x NVIDIA L40`
- Environment mode: `ENV_TYPE=user`
- Python: `/opt/miniconda3/bin/python3`
- Remote project dir: `~/ssd/da_final_project`
- Run name: `da_epoch_curve_info_v1`
- Slurm job id: `8860`
- Planned script: `scripts/submit_da_epoch_curve_l40.slurm`

## Launch Command

```bash
cd ~/ssd/da_final_project

ENV_TYPE=user \
PYTHON_BIN=/opt/miniconda3/bin/python3 \
RUN_NAME=da_epoch_curve_info_v1 \
CHECKPOINT_DIR=outputs/da_host_openloop_curve_v1/epoch_checkpoints \
EVAL_SEQUENCES=64 \
OBSERVATION_KIND=full \
OBSERVE_EVERY=2 \
OBSERVATION_NOISE=0.02 \
ENSEMBLE_SIZE=8 \
LATENT_NOISE=0.03 \
HIDDEN_NOISE=0.01 \
CELL_NOISE=0.01 \
sbatch --parsable scripts/submit_da_epoch_curve_l40.slurm
```

Current value:

- `JOB_ID=8860`

## Results To Retrieve

1. `slurm_da_curve_<job_id>.out`
2. `slurm_da_curve_<job_id>.err`
3. `outputs/da_epoch_curve_info_v1/da_epoch_curve_metrics.json`
4. `outputs/da_epoch_curve_info_v1/da_epoch_curve.png`
5. `outputs/da_epoch_curve_info_v1/da_information_curve.png`

## Retrieval Commands

```bash
ssh -p 2222 yudanni@172.16.43.21
squeue -j <job_id>
```

## Retrieved Result

- Job `8860` is queued in `compute`.

## Interpretation

This refresh should not change the RMSE/PCC values for the epoch sweep. Its purpose is to surface the DA-side uncertainty and information metrics across the same checkpoints.

## Suggested Next Step

1. Retrieve the new `da_information_curve.png`.
2. Interpret the trend of entropy, relative entropy, and mutual information together with RMSE/PCC.
3. Use that combined view to motivate selective assimilation.

## Notes

- The saved host checkpoints already exist on the server, so no retraining is needed.
- The local evaluator script was updated and syntax-checked before submission.
