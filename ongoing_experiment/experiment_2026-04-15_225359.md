# Experiment Report: `openloop_host_repair_v2_long`

## Goal

Run a longer overnight training job for the repaired open-loop host so we can see whether the improved predictor keeps getting better after the first 60-epoch refresh. This run keeps the upgraded architecture and loss setup but increases the epoch budget substantially.

## Date

- Start time: `2026-04-15 22:53:59`

## Status

Completed.

## Experiment Type

- Category: long open-loop host training
- Model: `OpenLoopVideoPredictor`
- Dataset: `Moving MNIST`
- Task: first `10` frames to next `10` frames prediction
- Purpose: overnight convergence check for the repaired host

## Submission Metadata

- Server: `172.16.43.21`
- SSH port: `2222`
- User: `yudanni`
- Partition: `compute`
- GPU request: `1 x NVIDIA L40`
- Environment mode: `ENV_TYPE=user`
- Python: `/opt/miniconda3/bin/python3`
- Remote project dir: `~/ssd/da_final_project`
- Run name: `openloop_host_repair_v2_long`
- Slurm job id: `8867`
- Slurm script: `scripts/submit_baseline_l40.slurm`

## Launch Command

```bash
cd ~/ssd/da_final_project

ENV_TYPE=user \
PYTHON_BIN=/opt/miniconda3/bin/python3 \
MODEL=openloop \
RUN_NAME=openloop_host_repair_v2_long \
EPOCHS=300 \
TRAIN_SEQUENCES=6000 \
VAL_SEQUENCES=1200 \
BATCH_SIZE=32 \
NUM_WORKERS=8 \
LATENT_DIM=96 \
HIDDEN_DIM=192 \
LR=0.0008 \
WEIGHT_DECAY=0.0001 \
PRED_L1_WEIGHT=1.0 \
PRED_BCE_WEIGHT=0.5 \
PRED_MSE_WEIGHT=0.15 \
TEACHER_FORCE_START=0.7 \
TEACHER_FORCE_END=0.05 \
GRAD_CLIP=1.0 \
SAVE_EVERY_EPOCH=1 \
sbatch --parsable scripts/submit_baseline_l40.slurm
```

## Results To Retrieve

1. `slurm_baseline_<job_id>.out`
2. `slurm_baseline_<job_id>.err`
3. `outputs/openloop_host_repair_v2_long/metrics.json`
4. `outputs/openloop_host_repair_v2_long/best_model.pt`
5. `outputs/openloop_host_repair_v2_long/sample_predictions.png`
6. `outputs/openloop_host_repair_v2_long/epoch_checkpoints/`

## Retrieval Commands

```bash
ssh -p 2222 yudanni@172.16.43.21
squeue -j 8867
```

```bash
ssh -p 2222 yudanni@172.16.43.21
cd ~/ssd/da_final_project
sed -n '1,220p' slurm_baseline_8867.out
sed -n '1,220p' slurm_baseline_8867.err
```

## Retrieved Result

- Slurm accepted the job as `8867`.
- Job `8867` completed on `ladhpc`.
- Final summary:
  - total epochs: `300`
  - best epoch by total validation loss: `49`
  - best validation metrics: `loss=0.2517`, `rmse=0.1500`, `pcc=0.7653`
  - last epoch: `300`
  - last validation metrics: `loss=0.2619`, `rmse=0.1430`, `pcc=0.7738`

## Interpretation

This run is the long-budget follow-up to `openloop_host_repair_v1`. It completed successfully. Compared with the 60-epoch run, it did not improve the best total validation loss, but it did continue to improve the late-epoch RMSE/PCC. That means longer training helped the frame metrics, while the total training objective still preferred an earlier checkpoint.

## Suggested Next Step

1. Pull `sample_predictions.png` and compare visual sharpness against `openloop_host_repair_v1`.
2. Decide whether the next DA run should use epoch `49` (best total loss) or a later checkpoint with stronger RMSE/PCC.
3. Refresh the latent ETKF evaluation using the selected repaired host checkpoint.

## Notes

- `8866` is the shorter 60-epoch repaired-host run that was already showing healthy progress by epoch 20.
- `8867` keeps the same repaired architecture and loss, but expands the epoch budget to `300`.
