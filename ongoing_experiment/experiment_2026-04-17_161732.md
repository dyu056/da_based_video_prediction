# Experiment Report: `da_eval_openloop_host_repair_v2_best_v1`

## Goal

Evaluate the repaired long-run open-loop host under latent ETKF so the latest predictor has a matching DA result, a qualitative open-loop-versus-DA image sequence, and updated report-ready metrics. The host checkpoint should follow the training objective, so this run uses the `best_model.pt` selected by the total validation loss from `openloop_host_repair_v2_long`.

## Date

- Start time: `2026-04-17 16:17:32`

## Status

Blocked by temporary server connectivity timeout before submission.

## Experiment Type

- Category: repaired-host DA evaluation
- Model: `OpenLoopVideoPredictor` + latent `ETKF`
- Dataset: `Moving MNIST`
- Task: first `10` frames to next `10` frames prediction with sparse noisy observations every two future steps
- Purpose: obtain the DA result that matches the repaired open-loop host used in the final report

## Submission Metadata

- Server: `172.16.43.21`
- SSH port: `2222`
- User: `yudanni`
- Partition: `compute`
- GPU request: `1 x NVIDIA L40`
- Environment mode: `ENV_TYPE=user`
- Python: `/opt/miniconda3/bin/python3`
- Remote project dir: `~/ssd/da_final_project`
- Run name: `da_eval_openloop_host_repair_v2_best_v1`
- Slurm script: `scripts/submit_da_experiment_l40.slurm`
- Checkpoint: `outputs/openloop_host_repair_v2_long/best_model.pt`

## Planned Launch Command

```bash
cd ~/ssd/da_final_project

ENV_TYPE=user \
PYTHON_BIN=/opt/miniconda3/bin/python3 \
RUN_NAME=da_eval_openloop_host_repair_v2_best_v1 \
CHECKPOINT=outputs/openloop_host_repair_v2_long/best_model.pt \
EVAL_SEQUENCES=128 \
BATCH_SIZE=1 \
NUM_WORKERS=8 \
OBSERVATION_KIND=full \
OBSERVE_EVERY=2 \
OBSERVATION_NOISE=0.02 \
ENSEMBLE_SIZE=8 \
LATENT_NOISE=0.03 \
HIDDEN_NOISE=0.01 \
CELL_NOISE=0.01 \
sbatch --parsable scripts/submit_da_experiment_l40.slurm
```

## Results To Retrieve

1. `slurm_da_<job_id>.out`
2. `slurm_da_<job_id>.err`
3. `outputs/da_eval_openloop_host_repair_v2_best_v1/metrics.json`
4. `outputs/da_eval_openloop_host_repair_v2_best_v1/comparison_predictions.png`
5. `outputs/openloop_host_repair_v2_long/metrics.json`
6. `outputs/openloop_host_repair_v2_long/sample_predictions.png`

## Retrieval Commands

```bash
ssh -p 2222 yudanni@172.16.43.21
squeue -u yudanni
```

```bash
ssh -p 2222 yudanni@172.16.43.21
cd ~/ssd/da_final_project
sed -n '1,220p' slurm_da_<job_id>.out
sed -n '1,220p' slurm_da_<job_id>.err
```

## Notes

- The matching host training run is `openloop_host_repair_v2_long` (Slurm `8867`).
- The selected checkpoint must follow the total validation loss criterion rather than the final epoch RMSE/PCC.
- A separate lightweight plotting step will generate the 300-epoch host training curves for the paper.

## Retrieval Attempt

- Attempted server command: submit `da_eval_openloop_host_repair_v2_best_v1` through `scripts/submit_da_experiment_l40.slurm`
- Result: could not reach `172.16.43.21:2222`
- Error: `ssh: connect to host 172.16.43.21 port 2222: Operation timed out`

## Interpretation

The repaired-host DA experiment is ready in terms of configuration, checkpoint choice, and local tracking, but the lab server was unreachable at submission time. This blocks the like-for-like ETKF rerun on the stronger host checkpoint.

## Suggested Next Step

1. Re-establish the VPN/server route.
2. Submit the exact launch command already recorded in this file.
3. Retrieve `comparison_predictions.png` and `metrics.json` as soon as the job finishes, then update the final paper with the repaired-host ETKF numbers.
